package database;

public class MapperController<T, K> {
	AbstractMapper<T, K> mapper;

	public MapperController(AbstractMapper<T, K> mapper) {
		super();
		this.mapper = mapper;
	}
	public void optionController(int opt){
		switch(opt){
			case 1:{
				break;
			}
		}
	}
	
}
